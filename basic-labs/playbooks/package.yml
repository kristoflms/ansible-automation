---
- name: Install a package
  gather_facts: true
  hosts: UBU1, UBU2
  tasks:
    - name: "Install apache httpd (state=present is optional)"
      ansible.builtin.apt:
        name: apache2
        state: present
